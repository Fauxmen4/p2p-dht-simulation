# TODO

## Конфигурация 

### Kademlia
поправить конфиг протокола, заиспользорвать нужные параметры на местах

### Bits
добавить в конфигурацию кол-во бит в идентификаторах
для этого надо по другому прокидывать конфиг (чтобы при генерации хэша учитывался этот параметр и использовалась нужная хэш-функция)
